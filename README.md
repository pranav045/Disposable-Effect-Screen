# Disposable-Effect-Screen
A simple Jetpack Compose demo showcasing DisposableEffect for lifecycle-aware side effects. Displays a screen with a toggle button, showing a toast when the screen appears and another when it’s removed. Demonstrates clean setup/cleanup handling in Compose UI.
